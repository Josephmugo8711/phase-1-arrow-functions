//Anonymous function is a function that
// does not have any name assosiated with it
const  divide = function() {
    return 2000 / 100
}

divide();

const square = (x) => x*x;

square();

const add = (a,b) => a + b;

add();